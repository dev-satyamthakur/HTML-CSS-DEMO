function cmp(a, b) {
    return a - b;
}

const arr = [3, 4, 2, 1, 700, 8, 5, 0, 9, 6]
arr.sort(cmp)
console.log(arr)

const fruits = ["apple", "mango", "orange", "grape", "cherry"]
fruits.sort()
console.log(fruits)